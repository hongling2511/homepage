# Homepage design direction

## 1. Visual theme and atmosphere

克制、精确、有判断力。暖纸色画布承载长内容，墨蓝只用于链接、关键数字和交互焦点，终端语言退居为少量技术签名。

## 2. Color palette and roles

- `--canvas`: `oklch(0.975 0.008 88)`, 页面底色
- `--surface`: `oklch(0.995 0.004 88)`, 浮层与重点内容
- `--ink`: `oklch(0.235 0.018 70)`, 主文字
- `--muted`: `oklch(0.49 0.014 72)`, 次级说明
- `--brand`: `oklch(0.35 0.075 252)`, 墨蓝链接与重点
- `--line`: `oklch(0.88 0.012 82)`, 分隔线

## 3. Typography rules

中文正文使用系统宋体与苹方组合，拉丁与代码使用 JetBrains Mono。显示字号 56/44/32/24，正文 17/15/13，中文正文行高 1.75。大标题只对拉丁字符做轻微负字距。

## 4. Component styling

按钮只分墨蓝实底和安静文字链接两级。证据面板采用纸面明度差和细分隔线，不堆叠统一圆角卡片。交互目标最小 40px，焦点态始终可见。

## 5. Layout principles

桌面端使用 12 列编辑式栅格，正文最大宽度 1120px。首屏左右不对称，能力证据采用 5:7 比例。移动端回到单列，所有 CTA 左对齐且保持自然宽度。

## 6. Depth and elevation

主要依靠背景明度层级，只有悬浮证据面板使用单一轻阴影。禁止玻璃拟态、彩色渐变和硬投影。

## 7. Do and don't

- 用项目机制与结果证明能力，不用空泛技能墙
- 墨蓝代表可点击或关键证据，不用于普通标题
- 保留少量命令行标签，不让页面变成终端模拟器
- 不使用三张同构营销卡片
- 不编造 AI 效率百分比
- 内部项目名统一匿名化

## 8. Responsive behavior

桌面基线 1280px，平板 880px，手机 480px。375px 检查完整中文与英文页面，320px 追加检查 CTA。导航在手机端收敛为核心锚点与简历入口。

## 9. Agent prompt guide

新增页面或组件时沿用暖纸、墨蓝、编辑式不对称布局。优先使用现有 Tailwind 类与 CSS token，不引入 CSS Modules、玻璃拟态、紫蓝渐变或另一套图标库。
